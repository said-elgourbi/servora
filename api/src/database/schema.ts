// Servora database schema.
//
// Business/domain tables are deliberately absent in this foundation milestone.
// Each future business feature declares its Drizzle schema here and adds a
// migration under api/drizzle/migrations (see docs/decisions/002-api.md).
//
// This file exists so `drizzle-kit generate` has a stable schema entry point
// and the migration folder/journal has a single authoritative source.
export {};
