<?xml version="1.0" encoding="utf-8"?>
<!--
  Pause glyph for the audio-evidence player (`BR-091`, ADR-018 A9).

  Geometry matches the icon set the design uses (lucide `Pause`). The control that draws it supplies the
  colour, so the stroke colour here is only a placeholder.
-->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="24dp"
    android:height="24dp"
    android:viewportWidth="24"
    android:viewportHeight="24">
    <path
        android:pathData="M9,4L9,20"
        android:strokeColor="#FFFFFFFF"
        android:strokeLineCap="round"
        android:strokeWidth="4" />
    <path
        android:pathData="M15,4L15,20"
        android:strokeColor="#FFFFFFFF"
        android:strokeLineCap="round"
        android:strokeWidth="4" />
</vector>
