<?xml version="1.0" encoding="utf-8"?>
<!--
  Crescent glyph for the dark option of the sign-in appearance controls.

  Geometry matches the icon set the design uses (`Figma/src/screens/Login.tsx`, lucide `Moon`):
  a stroked crescent rather than a filled one, so the two options read as one icon family. The
  control that draws it supplies the colour.
-->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="24dp"
    android:height="24dp"
    android:viewportWidth="24"
    android:viewportHeight="24">
    <path
        android:pathData="M12,3a6,6 0 0,0 9,9a9,9 0 1,1 -9,-9Z"
        android:strokeColor="#FFFFFFFF"
        android:strokeLineCap="round"
        android:strokeLineJoin="round"
        android:strokeWidth="1.75" />
</vector>
