<?xml version="1.0" encoding="utf-8"?>
<!--
  Chevron glyph marking the Province control as opening a picker.

  Geometry matches the icon set the design uses (`Figma/src/screens/Customers.tsx`, lucide
  `ChevronDown`). The control that draws it supplies the colour, so the stroke colour here is only a
  placeholder.
-->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="24dp"
    android:height="24dp"
    android:viewportWidth="24"
    android:viewportHeight="24">
    <path
        android:pathData="M6,9L12,15L18,9"
        android:strokeColor="#FFFFFFFF"
        android:strokeLineCap="round"
        android:strokeLineJoin="round"
        android:strokeWidth="2" />
</vector>
